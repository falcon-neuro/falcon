[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ben", email = "benfrost42@icloud.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
python = "python:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
